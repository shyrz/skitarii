# 构建上下文精简：镜像里的依赖由 pnpm install 重建，宿主机的 node_modules 可能带错平台的原生
# 二进制（如 macOS），拷进镜像会让后续构建不可用。
node_modules
**/node_modules
**/dist

# 本地配置与版本库不进镜像，.env 尤其不能烤进去。
.git
.gitignore
.env
.env.*
.workspace

**/.DS_Store
*.log
